import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const MusterCreationForm = () => {
  // holds state for multiple sections of the form.
  return (
    <View>
      <Text>MusterCreationForm</Text>
    </View>
  );
};

export default MusterCreationForm;

const styles = StyleSheet.create({});
